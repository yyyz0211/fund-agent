langchain>=0.3,<0.4
langchain-openai>=0.2,<0.3
# langgraph ecosystem pin (verified via pip 26.1.2 dry-run + 261 tests pass):
# - langgraph 1.0.0/1.0.1: only 1.0.x releases that allow langgraph-prebuilt>=1.0.0
#   (1.0.2+ tightened to >=1.0.2, which requires langchain-core>=1.0 and conflicts
#   with langchain 0.3's <1.0 ceiling on core)
# - langgraph-prebuilt 1.0.0: only release in the 1.0.x line that allows
#   langchain-core<1.0 (1.0.2+ requires core>=1.0; 1.0.13 requires core>=1.3.1).
#   Also 1.0.0 is the last release whose wheel actually ships tool_node.py
#   intact (later 1.0.x+ / 1.1.x ships a namespace with broken RECORD refs).
# - langgraph-checkpoint 2.1.x: required because langgraph-prebuilt==1.0.0
#   hard-requires checkpoint<3.0,>=2.1. langgraph-checkpoint 3.x+ (used by
#   langgraph 1.2.x) is incompatible with prebuilt 1.0.0.
# - langgraph-sdk 0.2.x: required by langgraph 1.0.1 (<0.3,>=0.2.2). The 0.4.x
#   line only ships against langgraph 1.2.x.
langgraph>=1.0.0,<1.0.2
langgraph-sdk>=0.2.2,<0.3
langgraph-prebuilt==1.0.0
langgraph-checkpoint>=2.1,<3.0
# langgraph-cli[inmem] is here (not in the Dockerfile) so pip resolves the
# whole constraint graph in one pass. Without this, the Dockerfile's
# `pip install "langgraph-cli[inmem]"` step would override the locked
# versions above (cli 0.3.5+ pulls in langgraph-api that demands
# langgraph-sdk>=0.4 etc.). cli 0.3.x pulls langgraph-api<0.3 (0.3.6) or
# <0.4 (0.3.7+) — both are compatible with our locked versions when
# resolved in the same resolution pass.
langgraph-cli[inmem]>=0.3,<0.4
akshare>=1.14
apscheduler>=3.10,<4.0
pandas>=2.0
pydantic-settings>=2.0
SQLAlchemy>=2.0,<3.0
pytest>=8.0
pytest-xdist>=3.6
fastapi>=0.138,<0.140
uvicorn[standard]>=0.27,<0.33
httpx>=0.27,<0.29
starlette>=1.0,<2.0
# Wave 1: 解析政府政策/披露页 HTML
selectolax>=0.3.21,<0.4
# PostgreSQL 数据库驱动。binary 版避免编译。
psycopg2-binary>=2.9
