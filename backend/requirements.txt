langchain>=0.3,<0.4
langchain-openai>=0.2,<0.3
langgraph>=0.2,<0.3
langgraph-sdk>=0.1,<0.2
# langgraph-prebuilt 1.1.0 ships an empty `langgraph.prebuilt` namespace whose
# RECORD references .py files that are NOT included in the wheel — a packaging
# bug. Pin to 1.0.x (the last release that actually ships tool_node.py) until
# the upstream issue is fixed. Pairs with langgraph-checkpoint>=4.1 for the
# `_DeltaSnapshot` symbol in langgraph 1.2.x.
langgraph-prebuilt>=1.0.13,<1.1
langgraph-checkpoint>=4.1,<5.0
akshare>=1.14
pandas>=2.0
pydantic-settings>=2.0
SQLAlchemy>=2.0,<3.0
pytest>=8.0
fastapi>=0.138,<0.140
uvicorn[standard]>=0.27,<0.33
httpx>=0.27,<0.29
starlette>=1.0,<2.0
